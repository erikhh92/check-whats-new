This tool scan for multiple URLs and search inside for existing keywords. The supported contents are JSON, PDF and estandar HTML for now.

When a keyword is found, the code sends a notification through a Telegram bot.
